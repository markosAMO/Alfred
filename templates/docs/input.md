---
source: {url or system reference}
type: {tracker_card | document | page | log | conversation}
fetched_at: {ISO timestamp}
fetched_by: {refine | diagnose}
---

# {identifier} — {title}

{Full text as written in the source. Navigation, styling and markup removed; everything
that carries meaning kept: description, acceptance criteria, comments, linked issue titles,
attachments described in words.}

## References not followed

{Links present in the source that do not change what gets built, recorded as text so the
decision not to follow them is visible.}
